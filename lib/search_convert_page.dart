import 'package:flutter/material.dart';

class SearchConvertPage extends StatelessWidget {
  const SearchConvertPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}