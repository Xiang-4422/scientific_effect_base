import 'package:flutter/material.dart';

class SearchParameterPage extends StatelessWidget {
  const SearchParameterPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
